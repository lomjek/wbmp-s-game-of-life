{
    "version": "0.2.0",
    "configurations": [
        

        {
            "name": "Run Go Version",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}"
        }
    ]
}